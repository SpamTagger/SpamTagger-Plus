<?php
/**
 * @license https://www.gnu.org/licenses/gpl-3.0.en.html
 * @package SpamTagger Plus
 * @author Olivier Diserens
 * @copyright 2025, SpamTagger
 */
?>
<div class="hoststatusbloc">
<?php foreach ($this->replicas as $replica) {?>
<div class="hostbloc" id='hostbloc_<?echo $replica->getId();?>'>
 <?php  echo $this->partial('ajax/monitorstatus/hoststatus.phtml', array(
                         'replica' => $replica,
                         'images_path' => $this->images_path,
                         't' => $this->t,
                         'columns' => $this->columns,
                         'pieLink' => $this->pieLink,
                         'initial_loading' => $this->initial_loading,
                         'salt' => $this->salt
)); ?>
</div>
<div class="hidden" id="hostbloc_<?echo $replica->getId();?>_hidden"></div>
<?php }?>
</div>
